"use client"

import { useState } from "react"
import { Dialog, DialogContent, DialogHeader, DialogTitle } from "@/components/ui/dialog"
import { Button } from "@/components/ui/button"
import { Badge } from "@/components/ui/badge"
import { Separator } from "@/components/ui/separator"
import { Copy, Download, Printer, Shield, CheckCircle2, AlertTriangle, Calculator } from "lucide-react"
import { VoteReceipt } from "@/lib/receipt-system"


export function VoteReceiptModal({ isOpen, onClose, receipt }) {
  const [copiedField, setCopiedField] = useState(null)

  if (!receipt) return null

  const copyToClipboard = async (text, field) => {
    try {
      await navigator.clipboard.writeText(text)
      setCopiedField(field)
      setTimeout(() => setCopiedField(null), 2000)
    } catch (error) {
      console.error("Failed to copy:", error)
    }
  }

  const downloadReceipt = () => {
    const receiptText = `
MSU-TCTO VOTING SYSTEM - VOTE RECEIPT
=====================================

Receipt ID: ${receipt.receiptId}
Date & Time: ${new Date(receipt.timestamp).toLocaleString()}
Election Type: ${receipt.electionType}
${receipt.college ? `College: ${receipt.college.toUpperCase()}` : ""}
Sequence Number: ${receipt.sequenceNumber}

VOTE SUMMARY:
${receipt.selections
  .map(
    (selection, index) =>
      `${index + 1}. ${selection.positionName}: ${selection.candidateName} (${selection.partyName})`,
  )
  .join("\n")}

SECURITY INFORMATION:
Vote Checksum: ${receipt.voteChecksum}
Verification Code: ${receipt.verificationCode}
Integrity Markers: ${receipt.integrityMarkers.join(", ")}
Digital Signature: ${receipt.digitalSignature}

VERIFICATION INSTRUCTIONS:
1. This receipt uses mathematical validation instead of cryptographic hashing
2. The vote checksum is calculated using position and candidate name values
3. Integrity markers provide multiple validation layers
4. Any tampering will cause verification to fail
5. Keep this receipt private to maintain ballot secrecy

IMPORTANT SECURITY NOTICE:
- This receipt is untraceable and maintains your anonymity
- Mathematical validation ensures data integrity without hashing
- Multiple verification layers protect against tampering
- You can verify this receipt at: [Your Domain]/receipt-verification
- Do not share this receipt with anyone

Generated by MSU-TCTO Voting System
    `.trim()

    const blob = new Blob([receiptText], { type: "text/plain" })
    const url = URL.createObjectURL(blob)
    const a = document.createElement("a")
    a.href = url
    a.download = `vote-receipt-${receipt.receiptId}.txt`
    document.body.appendChild(a)
    a.click()
    document.body.removeChild(a)
    URL.revokeObjectURL(url)
  }

  const printReceipt = () => {
    const printWindow = window.open("", "_blank")
    if (!printWindow) return

    const printContent = `
      <!DOCTYPE html>
      <html>
        <head>
          <title>Vote Receipt - ${receipt.receiptId}</title>
          <style>
            body { font-family: 'Courier New', monospace; margin: 20px; line-height: 1.6; }
            .header { text-align: center; border-bottom: 2px solid #000; padding-bottom: 10px; margin-bottom: 20px; }
            .section { margin-bottom: 20px; }
            .label { font-weight: bold; }
            .warning { background: #fff3cd; border: 1px solid #ffeaa7; padding: 10px; margin-top: 20px; }
            .security { background: #e8f5e8; border: 1px solid #4caf50; padding: 10px; margin: 10px 0; }
            .code { word-break: break-all; font-size: 10px; }
          </style>
        </head>
        <body>
          <div class="header">
            <h2>MSU-TCTO VOTING SYSTEM</h2>
            <h3>VOTE RECEIPT</h3>
          </div>
          
          <div class="section">
            <div><span class="label">Receipt ID:</span> ${receipt.receiptId}</div>
            <div><span class="label">Date & Time:</span> ${new Date(receipt.timestamp).toLocaleString()}</div>
            <div><span class="label">Election Type:</span> ${receipt.electionType}</div>
            ${receipt.college ? `<div><span class="label">College:</span> ${receipt.college.toUpperCase()}</div>` : ""}
            <div><span class="label">Sequence Number:</span> ${receipt.sequenceNumber}</div>
          </div>

          <div class="section">
            <div class="label">VOTE SUMMARY:</div>
            ${receipt.selections
              .map(
                (selection, index) =>
                  `<div>${index + 1}. ${selection.positionName}: ${selection.candidateName} (${selection.partyName})</div>`,
              )
              .join("")}
          </div>

          <div class="section security">
            <div class="label">SECURITY INFORMATION:</div>
            <div><span class="label">Vote Checksum:</span> ${receipt.voteChecksum}</div>
            <div><span class="label">Verification Code:</span> ${receipt.verificationCode}</div>
            <div><span class="label">Integrity Markers:</span> ${receipt.integrityMarkers.join(", ")}</div>
            <div><span class="label">Digital Signature:</span><br><span class="code">${receipt.digitalSignature}</span></div>
          </div>

          <div class="warning">
            <strong>IMPORTANT SECURITY NOTICE:</strong><br>
            • This receipt uses mathematical validation instead of cryptographic hashing<br>
            • Mathematical checksums ensure data integrity without hashing<br>
            • Multiple verification layers protect against tampering<br>
            • Keep this receipt confidential to protect your privacy<br>
            • This receipt is untraceable and maintains your anonymity
          </div>
        </body>
      </html>
    `

    printWindow.document.write(printContent)
    printWindow.document.close()
    printWindow.print()
  }

  return (
    <Dialog open={isOpen} onOpenChange={onClose}>
      <DialogContent className="max-w-2xl max-h-[90vh] overflow-y-auto">
        <DialogHeader>
          <DialogTitle className="flex items-center gap-2">
            <Shield className="w-5 h-5 text-green-600" />
            Vote Receipt Generated
          </DialogTitle>
        </DialogHeader>

        <div className="space-y-6">
          {/* Security Notice */}
          <div className="bg-amber-50 border border-amber-200 rounded-lg p-4">
            <div className="flex items-start gap-3">
              <AlertTriangle className="w-5 h-5 text-amber-600 mt-0.5 flex-shrink-0" />
              <div className="text-sm">
                <p className="font-semibold text-amber-800 mb-1">Important Security Notice</p>
                <p className="text-amber-700">
                  This receipt uses mathematical validation instead of cryptographic hashing. It maintains complete
                  anonymity while ensuring data integrity through multiple verification layers.
                </p>
              </div>
            </div>
          </div>

          {/* Mathematical Validation Info */}
          <div className="bg-blue-50 border border-blue-200 rounded-lg p-4">
            <div className="flex items-start gap-3">
              <Calculator className="w-5 h-5 text-blue-600 mt-0.5 flex-shrink-0" />
              <div className="text-sm">
                <p className="font-semibold text-blue-800 mb-1">Mathematical Validation</p>
                <p className="text-blue-700">
                  This receipt uses mathematical checksums, sequence validation, and integrity markers to ensure
                  authenticity without cryptographic hashing.
                </p>
              </div>
            </div>
          </div>

          {/* Receipt Information */}
          <div className="space-y-4">
            <div className="grid grid-cols-2 gap-4">
              <div>
                <label className="text-sm font-medium text-gray-600">Receipt ID</label>
                <div className="flex items-center gap-2 mt-1">
                  <code className="text-sm bg-gray-100 px-2 py-1 rounded">{receipt.receiptId}</code>
                  <Button
                    size="sm"
                    variant="ghost"
                    onClick={() => copyToClipboard(receipt.receiptId, "receiptId")}
                    className="h-6 w-6 p-0"
                  >
                    {copiedField === "receiptId" ? (
                      <CheckCircle2 className="w-3 h-3 text-green-600" />
                    ) : (
                      <Copy className="w-3 h-3" />
                    )}
                  </Button>
                </div>
              </div>

              <div>
                <label className="text-sm font-medium text-gray-600">Sequence Number</label>
                <p className="text-sm mt-1 font-mono">{receipt.sequenceNumber}</p>
              </div>
            </div>

            <div className="grid grid-cols-2 gap-4">
              <div>
                <label className="text-sm font-medium text-gray-600">Date & Time</label>
                <p className="text-sm mt-1">{new Date(receipt.timestamp).toLocaleString()}</p>
              </div>

              <div>
                <label className="text-sm font-medium text-gray-600">Election Type</label>
                <Badge variant="outline" className="mt-1">
                  {receipt.electionType === "ssc"
                    ? "Supreme Student Council"
                    : receipt.electionType === "college"
                      ? "College Officers"
                      : receipt.electionType}
                </Badge>
              </div>
            </div>

            {receipt.college && (
              <div>
                <label className="text-sm font-medium text-gray-600">College</label>
                <Badge variant="outline" className="mt-1">
                  {receipt.college.toUpperCase()}
                </Badge>
              </div>
            )}
          </div>

          <Separator />

          {/* Vote Summary */}
          <div>
            <h3 className="font-semibold text-gray-800 mb-3">Vote Summary</h3>
            <div className="space-y-3">
              {receipt.selections.map((selection, index) => (
                <div key={index} className="bg-gray-50 rounded-lg p-3">
                  <div className="flex justify-between items-start">
                    <div>
                      <p className="font-medium text-gray-800">{selection.positionName}</p>
                      <p className="text-sm text-gray-600">
                        {selection.candidateName} • {selection.partyName}
                      </p>
                    </div>
                    <Badge variant="secondary">{index + 1}</Badge>
                  </div>
                </div>
              ))}
            </div>
          </div>

          <Separator />

          {/* Security Information */}
          <div>
            <h3 className="font-semibold text-gray-800 mb-3">Security Information</h3>
            <div className="space-y-3">
              <div>
                <label className="text-sm font-medium text-gray-600">Verification Code</label>
                <div className="flex items-center gap-2 mt-1">
                  <code className="text-sm bg-gray-100 px-2 py-1 rounded font-mono">{receipt.verificationCode}</code>
                  <Button
                    size="sm"
                    variant="ghost"
                    onClick={() => copyToClipboard(receipt.verificationCode, "verificationCode")}
                    className="h-6 w-6 p-0"
                  >
                    {copiedField === "verificationCode" ? (
                      <CheckCircle2 className="w-3 h-3 text-green-600" />
                    ) : (
                      <Copy className="w-3 h-3" />
                    )}
                  </Button>
                </div>
                <p className="text-xs text-gray-500 mt-1">Mathematical validation code for receipt authenticity</p>
              </div>

              <div>
                <label className="text-sm font-medium text-gray-600">Vote Checksum</label>
                <div className="flex items-center gap-2 mt-1">
                  <code className="text-sm bg-gray-100 px-2 py-1 rounded font-mono">{receipt.voteChecksum}</code>
                  <Button
                    size="sm"
                    variant="ghost"
                    onClick={() => copyToClipboard(receipt.voteChecksum, "voteChecksum")}
                    className="h-6 w-6 p-0"
                  >
                    {copiedField === "voteChecksum" ? (
                      <CheckCircle2 className="w-3 h-3 text-green-600" />
                    ) : (
                      <Copy className="w-3 h-3" />
                    )}
                  </Button>
                </div>
                <p className="text-xs text-gray-500 mt-1">Mathematical checksum of your vote data</p>
              </div>

              <div>
                <label className="text-sm font-medium text-gray-600">Integrity Markers</label>
                <div className="mt-1 space-y-1">
                  {receipt.integrityMarkers.map((marker, index) => (
                    <code key={index} className="text-xs bg-gray-100 px-2 py-1 rounded font-mono block">
                      {marker}
                    </code>
                  ))}
                </div>
                <p className="text-xs text-gray-500 mt-1">Multiple validation layers for tamper detection</p>
              </div>
            </div>
          </div>

          {/* Action Buttons */}
          <div className="flex flex-col sm:flex-row gap-3 pt-4">
            <Button onClick={downloadReceipt} className="flex-1 gap-2">
              <Download className="w-4 h-4" />
              Download Receipt
            </Button>
            <Button onClick={printReceipt} variant="outline" className="flex-1 gap-2">
              <Printer className="w-4 h-4" />
              Print Receipt
            </Button>
          </div>

          {/* Verification Instructions */}
          <div className="bg-green-50 border border-green-200 rounded-lg p-4">
            <div className="flex items-start gap-3">
              <Shield className="w-5 h-5 text-green-600 mt-0.5 flex-shrink-0" />
              <div className="text-sm">
                <p className="font-semibold text-green-800 mb-1">Receipt Verification</p>
                <p className="text-green-700">
                  You can verify this receipt&apos;s authenticity using mathematical validation. The system checks multiple
                  integrity layers without using cryptographic hashing.
                </p>
              </div>
            </div>
          </div>
        </div>
      </DialogContent>
    </Dialog>
  )
}
